#include <stdio.h>

struct entry {
    char fname[10];
    char lname[10];
    char phone[12];
};

int main(void)
{
    // declare array structure with 1000 elements for entry
    struct entry list[1000];

    return 0;
}